name: CI Read Service

on:
  push:
    paths:
      - "photo-prestiges/read-service/**"
  pull_request:
    branches:
      - main
    paths:
      - "photo-prestiges/read-service/**"

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  test-and-lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./photo-prestiges/read-service

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: photo-prestiges/read-service/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        uses: ArtiomTr/jest-coverage-report-action@v2
        with:
          working-directory: photo-prestiges/read-service
          test-script: npm test -- --coverage --coverageReporters=json

      - name: Lint
        run: npm run lint
