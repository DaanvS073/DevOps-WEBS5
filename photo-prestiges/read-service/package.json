{
  "name": "read-service",
  "version": "1.0.0",
  "description": "Read Service for Photo Prestiges",
  "main": "./bin/www",
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon --legacy-watch -r dotenv/config ./bin/www",
    "test": "jest --coverage",
    "lint": "npx eslint ."
  },
  "dependencies": {
    "amqplib": "^0.10.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^4.3.2",
    "dotenv": "^16.4.1",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node",
    "preset": "@shelf/jest-mongodb",
    "coveragePathIgnorePatterns": ["/node_modules/"]
  }
}
